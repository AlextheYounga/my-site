<nav class="py-8 fancy-font <% if current_page?(root_path) %> bg-transparent
                            <% else %> bg-grey-lightest <% end %>">
    <div class="container flex justify-between mx-auto">
        <div class="w-1/2"><%= link_to "Home", root_path, class: "text-teal-darker no-underline text-2xl"%></div>
        <div class="w-1/2 flex justify-between">
            <%= link_to "Blog", articles_path, class: "text-teal-darker no-underline text-2xl"%>
            <%= link_to "About", about_path, class: "text-teal-darker no-underline text-2xl"%>
            <%= link_to "Reading List", readinglist_path, class: "text-teal-darker no-underline text-2xl"%>
            <%= link_to "Projects", projects_path, class: "text-teal-darker no-underline text-2xl"%>
            <%= link_to "Playground", playground_path, class: "text-teal-darker no-underline text-2xl"%>
            <% if logged_in? %>
                <%= link_to "Log Out", logout_path, method: :delete, class: "text-teal-darker no-underline text-2xl"%>
            <% else %>
                <%= nil %>
            <% end %>
        </div>
    </div>
</nav>

